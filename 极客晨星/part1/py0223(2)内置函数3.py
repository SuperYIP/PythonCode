#coding=utf-8
'''
课件
range(): 返回的是一个可迭代对象（类型是对象），而不是列表类型， 所以打印的时候不会打印列表。
reverse() 函数用于反向序列中元素，可以是元组，字符串，列表或range。
sorted() 函数对所有可迭代的对象进行排序操作。
slice() 函数实现切片对象，主要用在切片操作函数里的参数传递。
'''
# str1 = 'ahc'
# print(list(reversed(str1)))

# print(sorted(str1))
# print(sorted({1:'d',5:'B',3:'s'}))