#coding=utf-8
'''
Turtle库是Python语言中一个很流行的绘制图像的函数库.
想象一个小乌龟，在一个横轴为x、纵轴为y的坐标系原点，(0,0)位置开始，它根据一组函数指令的控制，
在这个平面坐标系中移动，从而在它爬行的路径上绘制了图形。
需要用到的命令：
forward(n)  : 前进n个像素
backward(n) : 后退n个像素
right(n)    : 向右转n度
left(n)     : 向左转n度
pensize(n)  : 调整画笔粗细，为n
color(颜色)  : 改变画笔颜色
penup()     : 提起画笔，此时不会在图中留下痕迹
pendown()   : 落下画笔，此时会在图中留下痕迹，默认为pendown()
goto(x,y)   : 将画笔移动到（x,y）坐标处
'''